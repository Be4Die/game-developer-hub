--- src/orchestrator/internal/transport/grpc/node.go
+++ src/orchestrator/internal/transport/grpc/node.go
@@ -302,13 +302,14 @@
 		ownerID = ""
 	}
 
-	if success, msg, err := h.nodeService.RestoreServiceBackup(ctx, ownerID, req.GetNodeId(), req.GetServiceName(), req.GetBackupId()); err != nil {
+	success, msg, err := h.nodeService.RestoreServiceBackup(ctx, ownerID, req.GetNodeId(), req.GetServiceName(), req.GetBackupId())
+	if err != nil {
 		return nil, domainError(err, "restore service backup")
 	}
 
 	return &pb.NodeServiceRestoreServiceBackupResponse{
-		Success: true,
-		Message: "Backup restored successfully",
+		Success: success,
+		Message: msg,
 	}, nil
 }
