--- src/frontend/src/features/manage-nodes/ui/ServiceBackupsModal.vue
+++ src/frontend/src/features/manage-nodes/ui/ServiceBackupsModal.vue
@@ -313,20 +313,19 @@
 
 defineEmits(['close']);
 
-const autoBackupEnabled = ref(
-  localStorage.getItem(`gdh_auto_backup_${props.nodeId}_${props.service.name}`) !== 'false'
-);
+const autoBackupEnabled = ref(props.service.auto_backup_enabled || false);
 
-function handleToggleAutoBackup() {
-  localStorage.setItem(
-    `gdh_auto_backup_${props.nodeId}_${props.service.name}`,
-    autoBackupEnabled.value ? 'true' : 'false'
-  );
-  if (autoBackupEnabled.value) {
-    successMessage.value = 'Автоматическое регулярное резервное копирование включено';
-  } else {
-    successMessage.value = 'Автоматическое регулярное резервное копирование отключено';
-  }
+async function handleToggleAutoBackup() {
+  try {
+    await nodeApi.toggleServiceAutoBackup(props.nodeId, props.service.name, autoBackupEnabled.value);
+    if (autoBackupEnabled.value) {
+      successMessage.value = 'Автоматическое регулярное резервное копирование включено';
+    } else {
+      successMessage.value = 'Автоматическое регулярное резервное копирование отключено';
+    }
+  } catch (err) {
+    error.value = 'Ошибка при переключении автобэкапов: ' + (err.response?.data?.message || err.message);
+    autoBackupEnabled.value = !autoBackupEnabled.value; // revert toggle
+  }
 }
 
 const backups = ref([]);
