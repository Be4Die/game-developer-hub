env: "local"
grpc:
  port: 50051
  timeout: 10s
db:
  host: "postgres"
  port: 5432
  user: "postgres"
  password: "postgres"
  name: "orchestrator" # единая БД, таблицы sso: users, sessions
  ssl_mode: "disable"
kv:
  addr: "valkey:6379"
  password: ""
  db: 1 # SSO использует отдельный DB index
  key_ttl: 30m
jwt:
  secret: "dev-jwt-secret-for-local-testing"
  access_token_ttl: 15m
  refresh_token_ttl: 720h
  issuer: "sso"
smtp:
  host: "localhost"
  port: 25
  from: "noreply@welwise.games"
  tls: false
email:
  verification_code_ttl: 24h
  reset_token_ttl: 1h
